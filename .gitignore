node_modules
dist
dist-ssr
*.local
.env
.env.local
.vercel
.playwright-mcp
*.tsbuildinfo

# Screenshots from local visual QA
*.png
!public/**/*.png
!src/**/*.png

# Client-supplied source photography. The processed WebP derivatives live in
# public/images and are committed; these originals are large, are the client's
# own files, and nothing in the build reads them.
update_photos/
